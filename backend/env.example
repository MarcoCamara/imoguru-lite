# ============================================
# CONFIGURAÇÃO LOCAL - Backend
# ============================================
# Copie este arquivo para .env e ajuste os valores

# Database (Supabase Local)
DB_HOST=127.0.0.1
DB_PORT=54322
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=postgres

# Database (Supabase Produção - se preferir)
# DB_HOST=db.xxxxx.supabase.co
# DB_PORT=5432
# DB_NAME=postgres
# DB_USER=postgres
# DB_PASSWORD=sua_senha_aqui

# JWT Secret (gere uma chave forte)
JWT_SECRET=desenvolvimento_local_secret_key_altere_em_producao

# Bootstrap Secret (para criar admin inicial)
BOOTSTRAP_SECRET=desenvolvimento_local_bootstrap_secret_altere_em_producao

# Resend API Key (opcional - para envio de emails)
RESEND_API_KEY=

# CORS - URL do frontend
CORS_ORIGIN=http://localhost:8085

# Porta do servidor
PORT=3001

# Ambiente
NODE_ENV=development

# ============================================
# INSTRUÇÕES:
# ============================================
# 1. Para desenvolvimento local com Supabase local:
#    - Execute: supabase start
#    - Use as credenciais padrão acima
#
# 2. Para usar Supabase de produção:
#    - Descomente as linhas do DB_HOST (produção)
#    - Comente as linhas do DB_HOST (local)
#    - Use as credenciais do painel do Supabase
#
# 3. Gere chaves fortes para produção:
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

