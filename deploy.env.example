# ============================================
# CONFIGURAÇÃO PRODUÇÃO/VPS - Docker Compose
# ============================================
# Copie este arquivo para deploy.env e ajuste os valores
# NUNCA COMMITE ESTE ARQUIVO COM CREDENCIAIS REAIS!

# ============================================
# 🌐 FRONTEND (Vite + Supabase)
# ============================================
VITE_SUPABASE_URL=https://seu-projeto.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sua_chave_publishable_aqui

# URL base para o backend (usada pelo frontend no build)
# IMPORTANTE: Use a URL pública do backend (ex: http://seu-vps-ip:3001 ou https://api.seudominio.com)
VITE_API_URL=http://localhost:3001

# ============================================
# 🧠 BACKEND (Node.js + PostgreSQL / Supabase)
# ============================================
DB_HOST=db.seu-projeto.supabase.co
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=sua_senha_do_supabase_aqui

# JWT (autenticação) - GERE UMA CHAVE FORTE!
JWT_SECRET=gerar_chave_forte_com_32_bytes_hex

# Resend (email transacional) - Opcional
RESEND_API_KEY=sua_chave_resend_aqui

# Bootstrap (chave interna) - GERE UMA CHAVE FORTE!
BOOTSTRAP_SECRET=gerar_chave_forte_com_32_bytes_hex

# ============================================
# ⚙️ CONFIGURAÇÕES GERAIS
# ============================================
NODE_ENV=production
TZ=America/Sao_Paulo
PORT=3001

# CORS - domínio autorizado (frontend publicado)
# IMPORTANTE: Use o domínio real do frontend (ex: https://seudominio.com)
CORS_ORIGIN=http://localhost:3000

# ============================================
# 🔧 PORTAS (Opcional - padrões já definidos)
# ============================================
# Frontend port (padrão: 3000)
FRONTEND_PORT=3000

# Backend port (padrão: 3001)
BACKEND_PORT=3001

# ============================================
# 📝 INSTRUÇÕES:
# ============================================
# 1. Copie este arquivo: cp deploy.env.example deploy.env
# 2. Preencha todas as variáveis com valores reais
# 3. Gere chaves fortes:
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 4. Configure CORS_ORIGIN com o domínio real do frontend
# 5. Configure VITE_API_URL com a URL pública do backend
# 6. NUNCA faça commit deste arquivo com credenciais!

